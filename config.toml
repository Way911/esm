src_url = "http://localhost:9200"
dest_url = "http://dest_host:9200"

src_index = "src_index_name"
dest_index = "dest_index_name"

# Number of workers to use, should be same as number of index partitions
worker_count = 3
size_per_page = 1000

bulk_size = 5000